# Examples

Synthetic examples live under this directory.

- `demo_fixture/` builds a no-token DuckDB demo that can run the research-source,
  contract, quality-audit, and dashboard commands end to end.

Generated outputs belong under `output/` and must not be committed.
